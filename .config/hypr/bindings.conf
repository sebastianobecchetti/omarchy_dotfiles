# Application bindings
$terminal = uwsm app -- $TERMINAL
$browser = omarchy-launch-browser
$fileM = nautilus
bindd = SUPER, E, File manager, exec, uwsm app -- nautilus --new-window
bindd = SUPER, B, Browser, exec, $browser
bindd = SUPER SHIFT, B, Browser (private), exec, $browser --private
bindd = SUPER, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER, N, Editor, exec, omarchy-launch-editor
bindd = SUPER, T, Activity, exec, $terminal -e btop
bindd = SUPER, D, Docker, exec, $terminal -e lazydocker
unbind = SUPER, Q
bindd = SUPER, Q, Terminal, exec, $terminal --working-directory="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER, C, exec, killactive
# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu
bind = SUPER, S, togglespecialworkspace, magic
bind = SUPER SHIFT, S, movetoworkspace, special:magic
bind = SUPER, L, exec, hyprlock
bind = SUPER SHIFT, L, exit
bind = SUPER, O, exec, $fileM ~/onedrive/Unistudium/
bind = SUPER, U, exec, bash -c 'unistudium-sync.sh &'
